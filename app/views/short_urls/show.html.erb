<div class="flex h-screen">
    <div class="m-auto w-4/5 max-w-2xl">

        <div class="text-center">
            <h1 class="text-4xl font-bold mb-2">Voila! Your shortened link</h1>
            <p>Copy the shortened link and share it in messages, texts, posts, websites and other locations.</p>
            <%= link_to "Shorten another link", root_path, class: "text-blue-600 hover:underline" %>
        </div>
        
        <div class="my-10">
            <div class="mb-6">
                <h2 class="text-xl font-bold mb-1">Short Link</h2>
                <%= link_to "#{request.protocol}#{request.host_with_port}/#{@short_url['short_code']}", 
                            "/#{@short_url['short_code']}", 
                            data: {turbo: false},
                            target: "_blank", 
                            class: "break-all text-blue-600 hover:underline" %>
                <p>
                    <i class="fa-regular fa-lightbulb"></i> 
                    Check out how your short link is doing with the 
                    <%= link_to "statistic", 
                                short_urls_statistic_path(@short_url.short_code), 
                                class: "text-blue-600 hover:underline" %>.
                </p>
            </div>
            <div class="mb-6">
                <h2 class="text-xl font-bold mb-1">Original Link</h2>
                <%= link_to @short_url['target_url'], 
                            @short_url['target_url'], 
                            target: "_blank",
                            class: "break-all text-blue-600 hover:underline" %>
            </div>
            <div class="mb-6">
                <h2 class="text-xl font-bold mb-1">Page Title</h2>
                <%= @short_url['title_tag'] %></span>
            </div>
        </div>        

    </div>    
</div>

