<h1>Welcome to GeckoURL</h1>

<%= form_with model: @url, url: "url/create" do |form| %>
    <div class="field">
        <%= form.label :target_url %>
        <%= form.text_field :target_url %>
    </div>
    <div class="actions">
        <%= form.submit %>
    </div>
<% end %>